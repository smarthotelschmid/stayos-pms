# Session 17 — 2026-04-26

## Überblick

Vier thematische Blöcke: (1) System-Status + Phase E Cosic-Cleanup ausgeführt,
(2) Calendar-Diagnose + Mini-Fix, (3) Stück 2 Magic Link vollständig implementiert
(Subtasks 2.1–2.5), (4) E2E Re-Trace aller 4 Szenarien — S3B von ❌ auf ✅.

---

## Block 1 — Cosic Phase E ausgeführt

Phase E war aus Session 16 pending (Backup vorhanden, wartete auf Go von Mathias).

**updateMany ausgeführt:**
```js
db.guests.updateMany(
  { _id: { $in: [/* 6 Cosic Sub-IDs */] } },
  { $set: { email: null, emailIsReal: false, emailIsFake: false, emailIsRealSince: null } }
)
```
**Ergebnis:** `modifiedCount: 6` ✅  
Backup lag unter `C:\stayos-pms\backups\cosic_sub_emails_pre_cleanup_2026-04-25.json`.

---

## Block 2 — Calendar-Diagnose + Fix

### Diagnose

**Frage:** Warum beginnt der erste sichtbare Eintrag im Calendar am 27. März?

**Ursache (Read-Only-Trace):**
- 515/516 Buchungen vor 27. März haben `isCsvImport: true` (src=beds24_csv)
- Backend filtert `isCsvImport: true` standardmäßig ohne `?includeHistorical=true`
- `Calendar.jsx` sendete diesen Parameter nie → alle CSV-Import-Buchungen unsichtbar
- 27. März ist kein Code-Cutoff, sondern der Beginn der Live-Buchungen (ab da: Beds24-API statt CSV)

**Mathias-Entscheid:** Fix ausführen — `&includeHistorical=true` permanent hinzufügen.

### Fix

**Commit:** `756c6b4` — `fix(calendar): add includeHistorical=true to both API call branches`  
**Datei:** `app/components/Calendar.jsx` Z.99–100  
**Deployed:** Vercel auto-deploy ✅

---

## Block 3 — Stück 2: Magic Link

**Gesamtziel:** Stellvertreter-Schutz (S3B-Bug) durch echte Email-Authentifizierung.
Kein Bypass von Profilen ohne Zugang zur Email-Adresse möglich.

**Architektur-Entscheide vor Implementierung:**
- Variante B für Task 2.4: Email-Match ohne Magic Token → HTTP 403 `magic_link_required`
- Option A für magicVerified-Transport: `magicToken` im POST /checkin Body, Backend re-verifiziert
- Calendar-Fix: `?includeHistorical=true` permanent (kein Schalter)

### Task 2.1 — MagicToken Schema + Backend Endpoints

**Commit:** `c3a548f`

**Neues Model:** `src/models/MagicToken.js`
```js
{ token, guestId, bookingId, tenantId, expiresAt, usedAt: null }
// TTL-Index auf expiresAt, 30-Min-Lifetime
```

**Neuer Endpunkt `POST /:token/request-magic-link`** (portal.js):
- Booking-Lookup + Status/Expiry-Check
- Same-tenant guestCol.findOne({ email, tenantId })
- Kein Treffer → `{ success: true, sent: false }`
- Treffer → MagicToken.create() + sendMagicLinkEmail() → `{ success: true, sent: true, maskedEmail }`

**Neuer Endpunkt `GET /:token/magic/:magicToken`** (portal.js):
- MagicToken-Lookup mit token + bookingId + tenantId
- Checks: exists, expiresAt > now, usedAt = null
- Fail → HTTP 410 `magic_token_invalid`
- Success → guestData (inkl. email) zurück; usedAt wird NICHT gesetzt (erst beim POST /checkin)

**Review-Fixes:**
- Redundanter `magicTokenSchema.index({ token: 1 })` entfernt (field-level unique: true reicht)
- Inline `require('../models/Settings')` im Handler entfernt (top-level already exists)
- Stille `return` bei SMTP-Fehler/Guest-not-found durch `throw new Error(...)` ersetzt

### Task 2.2 — Magic Link Email Template

**Commit:** `c3a548f`

**Neuer Service:** `src/services/magicLinkEmailService.js`
- 13 Sprachen (de, en, nl, fr, it, es, pl, cs, sk, hu, ro, hr, sl)
- `sendMagicLinkEmail({ tenantId, guestId, bookingToken, magicToken, hotelName, lang })`
- Button-Farbe: `#1a56db`
- URL: `${FRONTEND_URL}/portal/${bookingToken}?magic=${magicToken}`
- Nutzt `wrapHtml()` + `sendEmail()` aus emailService.js

### Task 2.3 — Frontend Magic Link Flow

**Commits:** `0a0b215` (Implementierung) + `257da2a` (3 Review-Fixes)

**Neue States in CheckInFlow.jsx:**
- `magicWaiting`, `magicToken`, `maskedEmail`, `magicError`

**Neues LABELS-Keys:**
- `magicLinkSentTitle`, `magicLinkSentBody` (mit `{{email}}`-Platzhalter), `magicLinkResend`, `magicLinkOtherEmail`, `magicLinkExpired`
- Alle 13 Sprachen; nicht-implementierte Sprachen erhalten englischen Fallback

**Ablauf-Änderungen:**
- `handleNext` Step 2 → `handleEmailStep()` statt direkt `handleLookup()`
- `handleEmailStep()`: POST /request-magic-link → `sent: true` → `magicWaiting`; `sent: false` → Fallthrough zu `handleLookup()`
- `nextDisabled` bei `step === 2 && magicWaiting`
- `handleResendMagicLink()`: re-POSTet /request-magic-link
- `useEffect([initialMagicToken])`: bei `?magic=` im URL → GET /magic/:magicToken → prefill + setStep(3) + email setzen; Fehler → `magicError = true`
- POST /checkin Body: `...(magicToken ? { magicToken } : {})`
- 403 `magic_link_required` in `handleSubmit`: automatisch /request-magic-link → `magicWaiting`

**page.jsx:** GuestPortalInner mit `useSearchParams()` in Suspense gewrappt; `magic`-Param an CheckInFlow

**3 Review-Fixes:**
1. "Andere E-Mail-Adresse"-Button: `setMaskedEmail('')` ergänzt
2. `if (j2.sent)` → `if (j2.success && j2.sent)` im 403-Handler
3. `useEffect`: `setEmail(d.email)` aus guestData → Resend-Button in magicError-Banner nur wenn `email` gesetzt

### Task 2.4 — Backend POST /checkin Magic-Verifikation

**Commit:** `12f6410`

**Änderungen in portal.js POST /:token/checkin:**

```js
// Body-Destructuring
const { guestData, invoiceRecipient, platformConsent, magicToken } = req.body;

// Nach Email-Lookup:
if (existingGuest && !magicToken) {
  return res.status(403).json({ success: false, error: 'magic_link_required' });
}
if (existingGuest && magicToken) {
  const mtDoc = await MagicToken.findOne({ token: magicToken, bookingId: booking._id, tenantId: TENANT_ID });
  if (!mtDoc || mtDoc.expiresAt < new Date() || mtDoc.usedAt != null) {
    return res.status(410).json({ success: false, error: 'magic_token_invalid' });
  }
  await MagicToken.updateOne({ _id: mtDoc._id }, { $set: { usedAt: new Date() } });
}
// Dann: normaler existingGuest-Update / neuer Gast — unverändert
```

**Kein magicToken bei neuem Gast nötig** (existingGuest = null → beide Guards übersprungen).

### Task 2.5 — E2E Re-Trace

Alle 4 Szenarien gegen live deploytes Code:

| Szenario | Ergebnis |
|---|---|
| S1 — Marina, Master-Link, eigene Daten | ✅ |
| S2 — Anna, Sub-Link, eigene Daten | ✅ |
| S3A — Markus, Master-Link, andere Email | ✅ |
| **S3B — Markus, Master-Link, Marinas Email** | **✅ (war ❌)** |

S3B-Weg blockiert: POST /request-magic-link → Link nur an Marina. Direkter /checkin ohne Token → 403. Markus kann Marina's Profil nicht mehr überschreiben.

---

## Deploy-Stand am Ende der Session

### Backend (`stayos-pms`)

| Commit | SHA | Inhalt |
|---|---|---|
| MagicToken + Endpoints + Email | `c3a548f` | models/MagicToken.js, services/magicLinkEmailService.js, portal.js (Endpoints) |
| Review Fix (email in GET response) | `bc894cd` | portal.js: email in guestData-Response |
| Magic-verifizierter POST /checkin | `12f6410` | portal.js: magic_link_required + token-verify |

**Server:** `git pull` + `pm2 restart stayos-api` ✅ online Port 3001

### Frontend (`stayos-dashboard`)

| Commit | SHA | Inhalt |
|---|---|---|
| Calendar includeHistorical | `756c6b4` | Calendar.jsx |
| Frontend Magic Link Flow | `0a0b215` | CheckInFlow.jsx, page.jsx |
| 3 Review-Fixes | `257da2a` | CheckInFlow.jsx |

**Vercel:** auto-deploy via git push ✅

---

## Bekannte Limitationen (aktualisiert)

S3B ist **nicht mehr** Known Limitation. Gelöst durch Stück 2.

---

## Hotfix nach Session 17

### Magic-Link FRONTEND_URL (2026-04-26)

**Bug:** `magicLinkEmailService.js` verwendete hardcoded Fallback
`'https://portal.smarthotel-schmid.at'` wenn `FRONTEND_URL` nicht gesetzt.
Gast Gabriel bekam Magic-Link mit falscher URL → Browser-Fehler.

**Fix:**
- `FRONTEND_URL=https://smarthotel-schmid.stayos.at` in `/var/www/stayos-api/.env` ergänzt
- Fallback entfernt, `throw new Error(...)` wenn ENV fehlt
- Commit: `b312e41` — `fix(magic-link): require FRONTEND_URL env, no silent fallback`

---

## Pending nach Session 17

| Item | Status | Notiz |
|---|---|---|
| Stück 3 — Cross-Tenant Profil-Match | Geplant | Separate Session |
| phoneNormalized | Separate Session | Schema-Feld + pre-save Hook + Lookup-Key |
| Sync-Refactor | Vor Founding-100 | PORTAL_OWNED_FIELDS aktiv, Refactor geplant |

### Backlog — SaaS Multi-Tenant URL-Resolution

Aktuell ist `FRONTEND_URL` global per ENV. Bei Multi-Tenant-Launch muss die URL
pro Tenant aus DB kommen (`tenant.portalDomain` oder `tenant.subdomain` +
Stayos-Default-Domain). `magicLinkEmailService` muss
`getFrontendUrlForTenant(tenantId)` aufrufen.
**Phase:** Multi-Tenant-Foundation, vor Founding-100.

### Backlog — i18n-Architektur

`languages.js` ist nur Sprachcodes/Flags-Liste. Portal-Übersetzungen liegen in
`CheckInFlow.jsx` LABELS-Objekt (nur DE/EN vollständig, andere 11 Sprachen
fallen auf EN zurück). Quick-Fixes als separate Konstanten (z.B. `CLOSE_HINTS`)
sind Anti-Pattern. Backend-Email-Templates haben dasselbe Problem.

Erforderlich vor Founding-100: echtes i18n-System (next-intl oder JSON-Files),
alle 13 Sprachen vollständig, Backend-Templates gleichziehen.

---

## Hard-Delete-Termin (soft-deleted Guest-Profile)

**Kontext:** 143.888 Guest-Profile sind soft-deleted (Duplikat-Cleanup nach
tenantId-Migration, Sessions 15/17). 100% haben `mergedInto`-Verweis.

**Diagnose 2026-04-28:**
- 109 Buchungen referenzieren noch soft-deleted guestId/bookedBy
- Späteste checkOut dieser Buchungen: **29. November 2026** (Schneider)
- Checkout-Verteilung: 2026-04-25 bis 2026-11-29

**Vorgehen am Hard-Delete-Tag:**

1. Atlas-Snapshot manuell triggern
2. Orphan-Check: `db.collection('bookings').countDocuments({ guestId: { $in: softDeletedIds } })` → muss 0 sein
3. Wenn 0: Hard-Delete in 1.000er Batches:
   ```js
   { tenantId: TENANT, deletedAt: { $exists: true, $ne: null } }
   ```
4. Wenn > 0: Lauf abbrechen, Orphan-Repair erst ausführen

**Ziel:** ~143.888 Profile löschen → ~485 aktive Profile übrig

**Ursprünglich geplant:** 30. April 2026  
**Verschoben auf:** **6. Dezember 2026** (späteste checkOut + 7 Tage Puffer)  
**Grund:** 109 aktive Buchungen mit Orphan-guestId bis 29.11.2026
